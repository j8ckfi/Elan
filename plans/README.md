# Implementation Plans

Generated by the improve skill on 2026-07-12 (audit focus: fork leftovers from
Mari, settings/agents UI, local demo mode). Planned at commit `84100c3`.
Each executor: read the plan fully before starting, honor its STOP conditions.
The advisor maintains this index during dispatched execution.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Purge the orphaned Mari chat shell and rebuild Settings as Elan's | P1 | M | — | DONE (approved 2026-07-12; worktree `.claude/worktrees/agent-a4a0e8f86ae25f0ec`, 3 commits, merged to main) |
| 002 | Obliterate local demo mode — the board is always host-backed | P1 | L | — | DONE (approved 2026-07-12 after 1 revision; worktree `.claude/worktrees/agent-ac53b9bd876cb0798`, 7 commits, merged to main. Bonus: fixed the tab-restore-loss bug local mode was masking; found relative-ELAN_STATE_DIR host bug — follow-up flagged) |
| 003 | The desktop app owns its host — Tauri spawns or adopts the Elan host | P1 | L | — | DONE (approved 2026-07-12; worktree `.claude/worktrees/agent-a0dc8de2b90b29366`, 2 commits, merged to main. Caveats: RunEvent::Exit cleanup verified by code review only — canonical Tauri pattern, but Cmd+Q not automatable headlessly; SIGTERM/pkill of the app leaks the host child (signals bypass Exit), mitigated by adopt-on-next-launch — follow-up flagged) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with reason) | REJECTED (with rationale)

## Dependency notes

- The three plans are **file-disjoint** and dispatched in parallel worktrees:
  001 = `src/components/chat/**` + `src/hooks` + `src/lib/settings.ts`;
  002 = `src/lib/board/**` + `RosterEditor`/`ConnectionBanner` + `e2e` + host endpoint;
  003 = `src-tauri/**` + build config.
- The user-visible payoff of 003 (desktop app that "just works") requires 002
  merged too — 002 makes the webview default to `127.0.0.1:4519`.
- 001 and 002 both render into the Settings dialog (002 edits RosterEditor,
  which 001's dialog embeds) — no file overlap, but re-check the Agents
  section after merging both.

## Findings considered and rejected

- **Mari mentions in code comments / `.mari-md` CSS namespace / `src/lib/agent`
  + `src/lib/adapters` + `src-tauri/src/pi.rs`**: by-design — AGENTS.md
  records the fork policy of keeping Mari's core intact. Renaming is churn,
  not polish.
- **`mari.projectOrder` / `mari.collapsedProjects` localStorage keys**: lived
  only in the orphaned SessionSidebar — dies with plan 001; no migration
  needed (keys are simply abandoned).
- **Wiring the `autoCheckUpdates` toggle instead of deleting it**: rejected —
  surfacing an app-level "update available" affordance is new UI outside the
  streamlining mandate; the manual check in Settings works today. Revisit as
  a direction item if release cadence picks up.
- **Renaming `dev/pi-bridge.ts` / chat-session dev loop**: still functional
  for adapter work on Mari's inherited core; untouched.
